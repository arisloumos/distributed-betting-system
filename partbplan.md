### Πλάνο Υλοποίησης (Android Frontend)

1.  **Βήμα 1: `SearchActivity` (UI & Logic)**
    *   Δημιουργία Activity με τα πεδία για τα φίλτρα (Stars, Category, Risk).
    *   Σύνδεση με τον `NetworkManager` για αποστολή του αντικειμένου `Filters`.
    *   *Στόχος:* Να στέλνουμε το αίτημα "SEARCH" και να λαμβάνουμε τη λίστα των παιχνιδιών.

2.  **Βήμα 2: `RecyclerView` (Η Λίστα των Παιχνιδιών)**
    *   Υλοποίηση του `RecyclerView` στο `SearchActivity`.
    *   Δημιουργία ενός `Adapter` για να εμφανίζουμε τα παιχνίδια που επιστρέφει ο Master (όνομα, ρίσκο, jackpot κλπ).
    *   *Στόχος:* Ο χρήστης να βλέπει τα αποτελέσματα της αναζήτησης.

3.  **Βήμα 3: `PlayActivity` (Ποντάρισμα)**
    *   Δημιουργία οθόνης όπου ο χρήστης επιλέγει το ποσό πονταρίσματος για το παιχνίδι που διάλεξε.
    *   Σύνδεση με τον `NetworkManager` για αποστολή του αιτήματος "PLAY".
    *   *Στόχος:* Να μπορεί ο παίκτης να ποντάρει και να βλέπει το αποτέλεσμα (Win/Loss).

4.  **Βήμα 4: Balance Management**
    *   Προσθήκη κουμπιών στο UI για "Add Balance" και "View Balance".
    *   Σύνδεση με τον `NetworkManager` για τα αιτήματα "ADD_BALANCE" και "GET_BALANCE".

5.  **Βήμα 5: Τελικός Έλεγχος & Asynchronous Handling**
    *   Έλεγχος ότι όλα τα requests γίνονται σε background threads (όπως ζητάει η εκφώνηση).
    *   Προσθήκη `ProgressBar` για να μην "παγώνει" το UI.
    *   Τελικό testing με τον Master Node να τρέχει στο PC.